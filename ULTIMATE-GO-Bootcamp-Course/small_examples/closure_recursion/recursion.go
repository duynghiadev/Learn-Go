func recursiveFunction(parameters) returnType {
	if baseCondition {
		return baseValue
	}
	return recursiveFunction(updatedParameters)
}

//you receive some values as paramters in the function
//you perform some operations on the parameters inside the function
// and the function itself is getting called again with the updated parameters
